{
  "name": "argo-wcs-ms",
  "version": "1.0.0",
  "description": "projet d'entretien fullstack MERN",
  "main": "server.js",
  "author": "Melanie Sarrouy",
  "license": "MIT",
  "type": "module",
    "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && yarn start",
    "build": "cd client && yarn build",
    "install-client": "cd client && yarn install",
    "heroku-postbuild": "yarn install-client && yarn build"
  },

  "dependencies": {
    "dotenv": "^16.0.2",
    "express": "^4.18.1",
    "mongoose": "^6.6.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "engines": {
    "node": "16.x"
  }
}
